# Data Structures and Algorithms (DSA) in Python

This is a repo to store DSA solutions and practice that I've completed thus far. This helps to reinforce practice of these concepts and allows me to revisit solutions and iterate upon them.

## Run
`uv sync`, then `uv run pytest` and `uv run ruff check .`
